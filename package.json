{
  "name": "uebungsbuchhaltung",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "check": "vitest run"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.3",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
